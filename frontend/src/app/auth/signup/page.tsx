import React from "react";
import { AuthForm as SignupForm } from "@/components/forms/auth-form";

const SignupPage = () => {
  return (
      <SignupForm title="Sign Up" buttonLabel="Create Account" />
  );
};

export default SignupPage;
